# Source of truth

This component is published from `ElevatorAI-Platform` (`services/vision`). The Platform repository is the integration source of truth; this repository exists so the Vision subsystem can be reviewed/cloned independently.
