# Source of truth

This component is published from `ElevatorAI-Platform` (`apps/web`). The Platform repository is the integration source of truth; this repository exists so the Web subsystem can be reviewed and cloned independently.
